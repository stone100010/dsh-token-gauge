# Assets

`docs/screenshot.png` is referenced by the root README. Capture the meter
against a dark GUI, crop to the panel plus a little surrounding UI so the
floating placement is visible, and keep it around 960px wide.
